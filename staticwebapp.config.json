{
  "routes": [
      {
          "route": "/api/*",
          "methods": ["GET", "POST", "PUT", "DELETE"],
          "allowedRoles": ["anonymous", "authenticated"],
          "rewrite": "/api"
      }
  ],
  "navigationFallback": {
      "exclude": ["/api/*"],
      "rewrite": "/index.html"
  }
}
